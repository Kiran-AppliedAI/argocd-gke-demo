apiVersion: v1
kind: Service
metadata:
  name: clientfacing-ui
  labels:
    app: clientfacing-ui
spec:
  selector:
    app: clientfacing-ui
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP

